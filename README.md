# High-Performance-Shallow-Water-PDE-Solver
Fast solver with parallelisation and optimisations. Solves in 0.2 seconds with 8 threads.

Use the terminal, compile and link with -fopenmp for parallelisation
